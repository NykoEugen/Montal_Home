// Google Maps Embed API - No JavaScript required for basic functionality
// The map is embedded via iframe in the HTML template

// If you need to add custom functionality later, you can add it here
document.addEventListener('DOMContentLoaded', function() {
    // Google Maps Embed API is self-contained and doesn't require JavaScript
    // for basic map display functionality
    
    // You can add custom functionality here if needed in the future
    // For example: custom markers, info windows, or map interactions
});
